import { useRouter } from "next/router";

const Blog = (props) => {
    const router = useRouter();
 
    return (
        <main>
            <h1>Blog</h1>
        </main>
    );
};

export default Blog;
